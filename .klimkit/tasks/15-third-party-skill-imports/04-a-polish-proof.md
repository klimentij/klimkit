# Imported Skill Polish Proof

## Checklist

- [x] Remove raw upstream-style frontmatter fields from imported `SKILL.md` files.
- [x] Make `klimkit-agent-browser` require screenshot-first visual verification for every new screen or material state.
- [x] Make `klimkit-web-design-guidelines` generic and avoid mutable Vercel-specific fetch instructions.
- [x] Make `klimkit-ui-ux-pro-max` concise and position it as a heuristic lookup pack rather than a giant always-loaded rule dump.
- [x] Make `klimkit-improve-codebase-architecture` use `klimkit-code-explorer`, `.klimkit` evidence paths, and `klimkit-grill-me` instead of upstream subagent/temp-report defaults.
- [x] Make `klimkit-impeccable` route through Klimkit implementation, browser proof, and walkthrough reporting while keeping bundled live automation opt-in.
- [x] Make `klimkit-antigravity-security-auditor` concise, scope-safe, and complementary to `klimkit-security-auditor`.

## Notes

The second commit intentionally edits only the imported skill instruction surfaces. Bundled references, scripts, and assets remain available for progressive disclosure, but the always-loaded `SKILL.md` files now describe Klimkit behavior instead of raw upstream workflows.
