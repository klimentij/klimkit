# Project Memory

Durable preferences, corrections, and process rules for Dominik-owned Klimkit project work. Add dated one-sentence memories.

## Memories

- 2026-05-14 Team workflow must distinguish the active operator's writable artifacts from other operators' readable, attributed project context.
- 2026-05-14 Per-project `.klimkit` migrations should work from inside the project checkout and also support explicit `--repo` and `--human-name` flags.
- 2026-05-14 In team workflow, a solo-style flat project `.klimkit` layout should be migrated to the current operator by default only after a clean dry run.
- 2026-05-14 Team workflow should derive the writable operator folder from `human_name` instead of requiring a second `artifact_owner` setting.

- 2026-05-14: In team workflow, existing parent-repo `.klimkit` evidence is treated as Klim-owned while this contribution's task evidence is written under `.klimkit/Dominik/`.
