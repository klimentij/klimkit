# Klim's directive — hosted, auth-protected HTML publishing by default

Received 2026-07-15, verbatim:

> Oh, by the way, web search the new features, artifacts for Claude Code, and sites for Codex. I want them to be the default to actually deploy HTML explainers and reports, and everything HTML in docs work for viewing by human driver. It should always be authentication protected using those native artifacts and sites tools. So made the necessary changes to AgentsMD and elsewhere if needed.
