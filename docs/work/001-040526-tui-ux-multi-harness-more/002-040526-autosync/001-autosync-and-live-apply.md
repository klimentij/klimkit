# Human Follow-Up: Autosync And Live Apply

Captured from the chat on 2026-05-04 for task history.

## Service Apply Correction

```text
kk should restart all that's needed when needed! and it should report what was restarted, what changes are live, what url etc
```

## Daemon Autosync Requirement

```text
actually i want deafult config to autosync on on all vm every n seconds (5 default). so whenever main has a new commit on top, daemon must notice that and pull and apply harness pack. if switchboard server or client are modified, must be updated code and restarted services! plan and implemen this. also add my last 2 messages to 07-h.. for history. 
once implemented, need finally needs to get pass decisions from 3 parallel final
  reviewer subagents before bothering me
```

## Telegram Autosync Notification

```text
and send a tg notification with summary of what auto-updated on what machine etc. short but very useful
```
